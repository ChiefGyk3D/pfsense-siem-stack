# pfSense Suricata Dashboard Configuration
# Copy this file to config.env and edit with your settings
# DO NOT commit config.env to git (it's in .gitignore)

# ============================================
# SIEM Server Configuration
# ============================================
# IP address or hostname of your SIEM server running OpenSearch, Logstash, and Grafana
SIEM_HOST=192.168.1.10

# OpenSearch
OPENSEARCH_PORT=9200

# Logstash
LOGSTASH_UDP_PORT=5140

# Grafana
GRAFANA_PORT=3000
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASS=admin

# ============================================
# pfSense Firewall Configuration
# ============================================
# IP address of your pfSense firewall
PFSENSE_HOST=192.168.1.1

# SSH user for pfSense (typically 'root')
PFSENSE_USER=root

# ============================================
# Forwarder Configuration
# ============================================
# Enable debug logging (creates large log files - only for troubleshooting)
DEBUG_ENABLED=false

# Forwarder debug log location (on pfSense)
DEBUG_LOG=/var/log/suricata_forwarder_debug.log

# ============================================
# Index Configuration
# ============================================
# Index name pattern (YYYY.MM.dd will be appended)
INDEX_PREFIX=suricata

# Data retention in days (0 = keep forever)
RETENTION_DAYS=30

# ============================================
# GeoIP Configuration
# ============================================
# GeoIP database path on pfSense
# If you have ntopng installed, it's at: /usr/local/share/ntopng/GeoLite2-City.mmdb
# Otherwise download from: https://github.com/PrxyHunter/GeoLite2
GEOIP_DB_PATH=/usr/local/share/ntopng/GeoLite2-City.mmdb
